   Sounds = JSON.parse(<%=
  sounds = {}
  Rails.root.join('app/assets/sounds').children.each do |sound|
    name = sound.basename.to_s
    sounds[name] = asset_url(name)
  end
  sounds.to_json.inspect
%>);
